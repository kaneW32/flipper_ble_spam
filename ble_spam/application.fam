App(
    appid="ble_spam",
    name="BLE Spam",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="ble_spam_app",   # Must match the function name, not the .c file
    stack_size=1024,
    requires=["gui", "input"],
    fap_category="Tools",
)
